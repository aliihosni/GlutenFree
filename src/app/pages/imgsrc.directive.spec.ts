import { ImgsrcDirective } from './imgsrc.directive';

describe('ImgsrcDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgsrcDirective();
    expect(directive).toBeTruthy();
  });
});
